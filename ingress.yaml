apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  name: explorecalifornia
spec:
  rules:
  - host: explorecalifornia
    http:
      paths:
      - backend:
          service:
            name: explorecalifornia
            port:
              number: 80
        path: /
        pathType: Exact
status:
  loadBalancer: {}
